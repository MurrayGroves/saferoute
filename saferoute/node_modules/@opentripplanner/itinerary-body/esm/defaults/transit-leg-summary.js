import coreUtils from "@opentripplanner/core-utils";
import PropTypes from "prop-types";
import React from "react";
import * as Styled from "../styled";
/**
 * This is a clickable component that summarizes the leg (travel time, stops
 * passed). On click it will expand and show the list of intermediate stops.
 */

export default function TransitLegSummary(_ref) {
  var leg = _ref.leg,
      onClick = _ref.onClick,
      stopsExpanded = _ref.stopsExpanded;
  return /*#__PURE__*/React.createElement(Styled.TransitLegSummary, {
    onClick: onClick
  }, leg.duration && /*#__PURE__*/React.createElement("span", null, "Ride ", coreUtils.time.formatDuration(leg.duration)), leg.intermediateStops && /*#__PURE__*/React.createElement("span", null, " / ", leg.intermediateStops.length + 1, " stops ", /*#__PURE__*/React.createElement(Styled.CaretToggle, {
    expanded: stopsExpanded
  })));
}
TransitLegSummary.propTypes = {
  leg: coreUtils.types.legType.isRequired,
  onClick: PropTypes.func.isRequired,
  stopsExpanded: PropTypes.bool.isRequired
};
//# sourceMappingURL=transit-leg-summary.js.map